{
  "$schema": "https://railway.app/railway.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "start": {
    "command": "cd backend && /opt/venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
